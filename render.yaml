services:
  - type: web
    name: buy-ya
    env: node
    rootDir: api
    buildCommand: npm install && cd ../admin && npm install && npm run build
    startCommand: npm start
    plan: starter
    autoDeploy: true